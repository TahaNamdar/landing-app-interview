import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "rgba(130, 0, 219, 1)",
      },
    },
  },
  plugins: [],
};

export default config;
